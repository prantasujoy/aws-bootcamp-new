#! /usr/bin/bash


#replace cruddr db name with ''

echo "dropping db"

NO_DB_CONNECTION_URL=$(sed 's/\/cruddr//g' <<<"$CONNECTION_URL")
psql $NO_DB_CONNECTION_URL -c "DROP DATABASE IF EXISTS cruddr;"