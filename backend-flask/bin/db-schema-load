#! /usr/bin/bash
echo "db-schema-load"

schema_path=$(realpath .)/db/schema.sql

if [ "$1" == "prod" ]; then
	CON_URL=$PROD_CONNECTION_URL
else
	CON_URL=$CONNECTION_URL
fi


psql $CON_URL cruddr < $schema_path